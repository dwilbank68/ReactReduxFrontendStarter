import React, { PropTypes } from 'react';

const welcome = (props) => {
  return (
    <div>
        Welcome View
    </div>
  );
};

welcome.propTypes = {};
welcome.defaultProps = {};

export default welcome;
